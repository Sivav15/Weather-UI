import React from "react";



const Item = ({ item, classes }) => {
    return (
        <div style={{ backgroundColor: "red" }} className='item'>
            {/* {item.content} */}
        </div>
    );
};

export default Item;
